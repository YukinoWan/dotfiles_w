#
# Executes commands at the start of an interactive session.
#

# Theme
PROMPT="[%F{green}%m%f-%F{yellow}(%~)%f]
$ "

# Alias
alias ls='ls -FH --color=tty'
alias ll='ls -lFHh --color=tty'
alias la='ls -laFHh --color=tty'
alias lt='ls -ltFHh --color=tty'
alias lat='ls -latFHh --color=tty'
